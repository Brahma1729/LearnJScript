let arr = ["Raj", "Praneeth", "Veera"]

// destructuring the array
// In Array values are stored with reference of indexes

let [a,b,c] = arr

// 'a' stores index 0th vale and so on 
//unlike objct destructuring for array destructuring we should use square braces

console.log(`${a} , ${b}, ${c}`);
