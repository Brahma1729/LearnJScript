// the names of all export members should be same in import statement also
import { hello, language, age } from "./exportDemo.js";

//hello("Veera")

console.log(`${hello("Veera")} is the learning ${language} at the age of ${age}`);